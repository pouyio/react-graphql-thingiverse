type Query {
  newest: [Thing]
  popular: [Thing]
  featured: [Thing]
}

type Thing {
  id: Int
  is_private: Boolean
  is_published: Boolean
  is_purchased: Boolean
  name: String
  public_url: Boolean
  thumbnail: String
  url: String
}